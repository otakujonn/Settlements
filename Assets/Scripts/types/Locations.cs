using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

// Rules for location spawning are in TileMap.GenerateLocationList
// Be sure to add additional rules there if neccesary

public enum Locations
{
    NONE,
    CITY,
    HARBOR,
    TOWER,
    OASIS,
    BARN,
    TAVERN,
    FARM,
    ORACLE,
    PADDOCK
}
